spring:
  application:
    name: role-service

  profiles:
    active: dev

  config:
    import: classpath:shared-config.yml

# OpenAPI configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
    url: /api-docs
    config-url: /api-docs/swagger-config

# Kafka Topics Configuration for Role Service
kafka:
  topics:
    # Topics that this service publishes to
    publish:
      role-created: "role-created"
      role-updated: "role-updated"
      role-assigned: "role-assigned"
      role-revoked: "role-revoked"
    
    # Topics that this service subscribes to
    subscribe:
      user-registered: "user-registered"
      saga-compensation: "saga-compensation"