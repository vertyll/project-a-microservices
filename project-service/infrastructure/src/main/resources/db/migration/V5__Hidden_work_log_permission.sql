-- Who may read a hidden entry follows from the role, which iam decides. Naming
-- roles per project said the same thing in a second place, and the two could
-- disagree; the project keeps only the switch.
DROP TABLE IF EXISTS project_hidden_work_log_role;
