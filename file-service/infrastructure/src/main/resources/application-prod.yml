spring:
  application:
    name: file-service

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: false

  flyway:
    validate-on-migrate: true
    clean-disabled: true

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
    consumer:
      group-id: file-service-group

server:
  port: ${SERVER_PORT:8088}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    ROOT: WARN
    com.vertyll.veds: INFO
    org.springframework.security: INFO
    org.springframework.kafka: INFO
    org.hibernate.SQL: INFO
    org.hibernate.type.descriptor.sql.BasicBinder: INFO

veds:
  translation:
    client:
      base-url: ${TRANSLATION_SERVICE_URL:http://translation-service:8087}
  file:
    storage:
      endpoint: ${OBJECT_STORAGE_ENDPOINT}
      region: ${OBJECT_STORAGE_REGION:garage}
      bucket: ${OBJECT_STORAGE_BUCKET:veds-files}
      access-key: ${OBJECT_STORAGE_ACCESS_KEY:veds-local-access-key}
      secret-key: ${OBJECT_STORAGE_SECRET_KEY:veds-local-secret-key}
      upload-url-validity: 15m
      download-url-validity: 5m
