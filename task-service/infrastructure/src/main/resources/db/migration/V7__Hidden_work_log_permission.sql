-- Who may read a hidden entry follows from the role, which iam decides and this
-- service already projects. The per-project list said the same thing in a second
-- place, and the two could disagree.
DROP TABLE IF EXISTS project_ref_hidden_work_log_role;
