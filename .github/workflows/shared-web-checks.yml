name: Shared Web Checks

on:
  push:
    branches:
      - main
    paths:
      - "shared-web/**"
      - "shared-error/**"
      - "shared-authz/**"
      - ".github/workflows/shared-web-checks.yml"
      - ".github/workflows/quality-checks.yml"
  pull_request:
    branches:
      - main
    paths:
      - "shared-web/**"
      - "shared-error/**"
      - "shared-authz/**"
      - ".github/workflows/shared-web-checks.yml"
      - ".github/workflows/quality-checks.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/quality-checks.yml
    with:
      service: shared-web
