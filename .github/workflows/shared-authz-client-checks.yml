name: Shared Authz Client Checks

on:
  push:
    branches:
      - main
    paths:
      - "shared-authz-client/**"
      - ".github/workflows/shared-authz-client-checks.yml"
      - ".github/workflows/quality-checks.yml"
  pull_request:
    branches:
      - main
    paths:
      - "shared-authz-client/**"
      - ".github/workflows/shared-authz-client-checks.yml"
      - ".github/workflows/quality-checks.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/quality-checks.yml
    with:
      service: shared-authz-client
